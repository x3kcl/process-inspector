<?xml version="1.0" encoding="UTF-8"?>
<definitions xmlns="http://www.omg.org/spec/BPMN/20100524/MODEL"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xmlns:flowable="http://flowable.org/bpmn"
             xmlns:bpmndi="http://www.omg.org/spec/BPMN/20100524/DI"
             xmlns:omgdc="http://www.omg.org/spec/DD/20100524/DC"
             xmlns:omgdi="http://www.omg.org/spec/DD/20100524/DI"
             typeLanguage="http://www.w3.org/2001/XMLSchema"
             expressionLanguage="http://www.w3.org/1999/XPath"
             targetNamespace="http://inspector.io/demo">

  <!-- FIX-PROC-06: call activity → demoFailingPayment child. Started with divisor=0 the
       CHILD dead-letters organically while the parent waits on the call activity — the
       failedInSubprocess roll-up fixture (child rows carry superProcessInstanceId).
       businessKey is inherited so key-based searches find the parent. Deploy the child
       definition FIRST (seed.sh orders this). -->
  <process id="demoParent" name="Demo Parent (failing child)" isExecutable="true">
    <startEvent id="start"/>
    <sequenceFlow id="flow1" sourceRef="start" targetRef="callPayment"/>
    <callActivity id="callPayment" name="Charge via child"
                  calledElement="demoFailingPayment"
                  flowable:inheritBusinessKey="true">
      <extensionElements>
        <flowable:in source="amount" target="amount"/>
        <flowable:in source="divisor" target="divisor"/>
      </extensionElements>
    </callActivity>
    <sequenceFlow id="flow2" sourceRef="callPayment" targetRef="end"/>
    <endEvent id="end"/>
  </process>

  <bpmndi:BPMNDiagram id="BPMNDiagram_demoParent">
    <bpmndi:BPMNPlane id="BPMNPlane_demoParent" bpmnElement="demoParent">
      <bpmndi:BPMNShape id="BPMNShape_start" bpmnElement="start">
        <omgdc:Bounds x="100" y="100" width="30" height="30"/>
      </bpmndi:BPMNShape>
      <bpmndi:BPMNShape id="BPMNShape_callPayment" bpmnElement="callPayment">
        <omgdc:Bounds x="180" y="85" width="100" height="60"/>
      </bpmndi:BPMNShape>
      <bpmndi:BPMNShape id="BPMNShape_end" bpmnElement="end">
        <omgdc:Bounds x="330" y="101" width="28" height="28"/>
      </bpmndi:BPMNShape>
      <bpmndi:BPMNEdge id="BPMNEdge_flow1" bpmnElement="flow1">
        <omgdi:waypoint x="130" y="115"/>
        <omgdi:waypoint x="180" y="115"/>
      </bpmndi:BPMNEdge>
      <bpmndi:BPMNEdge id="BPMNEdge_flow2" bpmnElement="flow2">
        <omgdi:waypoint x="280" y="115"/>
        <omgdi:waypoint x="330" y="115"/>
      </bpmndi:BPMNEdge>
    </bpmndi:BPMNPlane>
  </bpmndi:BPMNDiagram>
</definitions>
