# Default compose profiles for the dev harness. NO SECRETS in this file.
# `docker compose -f docker/docker-compose.dev.yml up -d` boots the 6.x pair;
# add engines with e.g. `--profile flowable-7` / `--profile postgres`
# (the CLI flag overrides this default).
COMPOSE_PROFILES=flowable-6
