# Merge gates (OPERATIONS §8): lint, unit ladder, frontend build, image build, and the
# dockerized-engine integration matrix. Zero-flake doctrine: matrix legs gate on
# docker/smoke-test.sh (bounded readiness probes) before Maven touches an engine.
name: ci

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  lint:
    runs-on: self-hosted
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: maven
      - name: Spotless check
        working-directory: backend
        run: mvn -B spotless:check
      - name: Security audit (env-ref secrets doctrine)
        run: ./scripts/security-audit.sh
      - name: Shell syntax check (deploy/CI scripts)
        run: ./scripts/shell-syntax-check.sh

  unit:
    runs-on: self-hosted
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: maven
      - name: Unit ladder (rungs 1-3 + ArchUnit sleep ban)
        working-directory: backend
        run: mvn -B test

  frontend:
    runs-on: self-hosted
    timeout-minutes: 20
    defaults:
      run:
        working-directory: frontend
    services:
      # The OpenAPI drift gate boots the REAL BFF. Engines may be absent (health degrades,
      # boot succeeds — ARCH §2.2), but Postgres is a hard boot dependency (Flyway +
      # ddl-auto=validate), so it runs as a service. Container-port-only mapping: Docker
      # assigns a random free HOST port (read via job.services below), so concurrent
      # frontend jobs on different runner slots never collide and no slot entry is needed.
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: inspector
          POSTGRES_USER: inspector
          POSTGRES_PASSWORD: inspector
        ports:
          - 5432/tcp
        options: >-
          --health-cmd "pg_isready -U inspector"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 20
    steps:
      - uses: actions/checkout@v4
      # Runner slots (docker/ci-runner/docker-compose.yml) carry a disjoint port block as
      # container env so concurrent jobs never collide; slot-1 values are the fallback for
      # a slot-less runner. scripts/free-port.sh double-checks the slot's own port is
      # actually free on THIS host and falls back within the slot's own band otherwise
      # (#163/#288 — a slot's fixed port can end up permanently squatted by something
      # unrelated; this makes that self-healing instead of needing a manual reassignment).
      - name: Resolve the slot port namespace
        working-directory: .
        run: echo "BFF_PORT=$(scripts/free-port.sh "${PI_BFF_PORT:-8086}")" >> "$GITHUB_ENV"
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - name: Install
        run: npm ci
      - name: Lint (ESLint strict-type-checked — hard failure, SPEC §10)
        run: npm run lint
      - name: Format check (Prettier — hard failure, SPEC §10)
        run: npm run format:check
      - name: Unit tests (vitest)
        run: npm test
      - name: Build (watermark + no-enterprise guards + tsc + vite)
        run: npm run build
      # ---- OpenAPI drift gate (R-SEM-15): cross-language drift is a CI failure. ----
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: maven
      - name: Build BFF jar (tests belong to the unit/integration jobs)
        working-directory: backend
        run: mvn -B -DskipTests package
      - name: Boot BFF in the background (no engines — degraded health is expected)
        working-directory: backend
        env:
          # password-refs must resolve; the engines they authenticate against are absent.
          ENGINE_A_PASSWORD: unused-in-ci
          ENGINE_B_PASSWORD: unused-in-ci
          # Slot remap: the BFF on the runner slot's PI_BFF_PORT (dev default is 8085) and
          # its DB on the service's Docker-assigned host port, so concurrent dev stacks AND
          # concurrent CI jobs on other slots all coexist.
          SERVER_PORT: ${{ env.BFF_PORT }}
          INSPECTOR_DB_URL: jdbc:postgresql://localhost:${{ job.services.postgres.ports['5432'] }}/inspector
        run: |
          JAR=$(ls target/*.jar | head -1)
          nohup java -jar "$JAR" > /tmp/bff.log 2>&1 &
          echo $! > /tmp/bff.pid
      - name: Wait for /v3/api-docs (bounded)
        run: |
          for _ in $(seq 1 90); do
            curl -fsS "http://localhost:${BFF_PORT}/v3/api-docs" > /dev/null && exit 0
            sleep 2
          done
          echo "::error::BFF did not serve /v3/api-docs within 180s"
          tail -200 /tmp/bff.log
          exit 1
      - name: Regenerate the API contract from the running BFF
        env:
          INSPECTOR_BFF_URL: http://localhost:${{ env.BFF_PORT }}
        run: npm run gen:api
      - name: Drift gate — committed schema must match the live contract
        run: |
          git diff --exit-code -- src/api/schema.d.ts || {
            echo "::error::frontend/src/api/schema.d.ts is stale — run the BFF, 'npm run gen:api', and commit the regenerated schema (never hand-edit it)."
            exit 1
          }
      - name: Dump BFF log
        if: failure()
        run: tail -200 /tmp/bff.log || true
      - name: Reap the background BFF
        # Defensive hygiene: the EPHEMERAL runner container is torn down and recreated per
        # job (docker/ci-runner: EPHEMERAL=true + restart:unless-stopped), so a leaked BFF
        # already cannot survive into the next run — this reaps it within-job regardless of
        # how the job exited, keeping the host :8086 free the moment the frontend job ends.
        if: always()
        run: kill "$(cat /tmp/bff.pid 2>/dev/null)" 2>/dev/null || true

  # Playwright + axe (#85, TEST-STRATEGY §1 R4: "axe accessibility checks hard-fail"). The
  # ~17 e2e/*.spec.ts specs are hermetic BY CONSTRUCTION (every /api call is page.route-
  # fulfilled — see the header comment in any spec) — no BFF, engine, or Postgres needed,
  # just the plain Vite dev server. Chromium + its OS deps are baked into the CI runner
  # image (docker/ci-runner/Dockerfile) rather than installed per-job: the runner container
  # is ephemeral and only /opt/hostedtoolcache survives a restart, so a per-job
  # `playwright install --with-deps` would re-download the ~200MB browser AND re-run
  # apt-get for its libs on every single run.
  e2e:
    runs-on: self-hosted
    timeout-minutes: 15
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      # Runner slots share the HOST network (network_mode: host) — each slot's Vite dev
      # server must bind a disjoint port or two concurrent e2e jobs (different PRs, same
      # instant) collide; slot-1's value is the fallback for a slot-less runner.
      # scripts/free-port.sh confirms the slot's port is actually free on this host and
      # falls back within the slot's own band otherwise (see the frontend job's step).
      - name: Resolve the slot port namespace
        working-directory: .
        run: echo "PI_E2E_PORT=$(scripts/free-port.sh "${PI_E2E_PORT:-4173}")" >> "$GITHUB_ENV"
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - name: Install
        run: npm ci
      - name: Playwright + axe (browser baked into the runner image, not installed here)
        run: npm run e2e
      - name: Upload the HTML report and traces on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: |
            frontend/playwright-report/
            frontend/test-results/
          retention-days: 7

  docker:
    runs-on: self-hosted
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - name: Build BFF image (multi-stage, not pushed)
        uses: docker/build-push-action@v6
        with:
          context: .
          push: false
          cache-from: type=gha
          # mode=max exported ALL build-stage layers (maven-deps/npm) and GitHub's cache
          # service uploading them has timed out this job 3x today (once on #337, twice on
          # #339 head 44ba1ec, run 30211616618 — a single layer write took 630s then the next
          # hung to the 20m ceiling) despite the image build itself finishing in ~2.5min.
          # mode=min only exports final-stage layers (small, fast); ignore-error=true means a
          # slow/broken cache service can never fail an otherwise-green build.
          cache-to: type=gha,mode=min,ignore-error=true

  integration:
    runs-on: self-hosted
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        include:
          # composeProfiles is explicit on every leg: most are "${profile},postgres", but the
          # dual-major findings leg boots two engine profiles at once (#362). "profile" is the
          # smoke-test.sh / CI_PROJECT alias and may differ from the compose profile set.
          - profile: flowable-6
            composeProfiles: "flowable-6,postgres"
            itClass: EngineHealthIT,SearchServiceIT,TriageAggregationIT,TriageCmmnScopeIT,CaseDetailIT,KwayPagingIT
          - profile: flowable-7
            composeProfiles: "flowable-7,postgres"
            itClass: EngineHealth7IT,Search7IT,Triage7IT,ExternalWorkerJob7IT,Kway7IT
          - profile: legacy
            composeProfiles: "legacy,postgres"
            itClass: EngineHealthLegacyIT,SearchLegacyIT,TriageLegacyIT,ExternalWorkerJobLegacyIT,TriageCmmnScopeLegacyIT
          # The Boot-layout leg: an APPLICATION embedding Flowable (flap) — /process-api with
          # root-level /cmmn-api + /external-job-api siblings, behind its own Basic-auth service
          # account. Neither shape can be exercised by the flowable-rest war images above, and
          # this inspector runs against exactly that shape in production. The engine image is
          # ghcr.io/x3kcl/flap:latest, republished by flap on every push to master; the package
          # grants this repo Actions read access, so the run's own GITHUB_TOKEN pulls it.
          - profile: flap
            composeProfiles: "flap,postgres"
            itClass: BootLayoutEngineIT
          # Dual-major typed-findings leg (#362): MigrationFindingsIT calibrates severity on
          # BOTH 6.8 and 7.1 in one suite (it-findings profile), so this leg boots both
          # compose profiles at once. "migration-findings" is a smoke-test alias, not a
          # compose profile. Costs one extra engine vs the flowable-6 leg (A+B already; +7);
          # engines run on the host daemon, outside the runner mem_limit.
          - profile: migration-findings
            composeProfiles: "flowable-6,flowable-7,postgres"
            itClass: MigrationFindingsIT
    env:
      # Job-level so EVERY compose invocation (up/exec/logs) sees the same profile set —
      # CLI --profile flags would scope only the command they're passed to, leaving
      # logs/exec on the docker/.env default.
      COMPOSE_PROFILES: ${{ matrix.composeProfiles }}
    steps:
      - uses: actions/checkout@v4
      # Self-hosted runners share hp04's Docker daemon with a live demo + interactive dev
      # stacks, so every published port is remapped off the dev defaults — and each runner
      # SLOT (docker/ci-runner/docker-compose.yml) carries its own disjoint remap block as
      # container env so concurrent jobs on different slots never collide either. The
      # compose file, smoke-test.sh, seed.sh and the IT base-urls all read PI_*_PORT from
      # the process environment; slot-1 values are the fallback for a slot-less runner.
      # scripts/free-port.sh double-checks each port is actually free on THIS host and
      # falls back within the slot's own band otherwise (#163/#288 — a slot's fixed port
      # can end up permanently squatted by something unrelated; this makes that
      # self-healing instead of needing a manual reassignment). Windows stay well inside
      # the ~100-port gap to the next slot's band, and each engine port is resolved
      # avoiding the ones already picked for its own siblings so two of A/B/7/legacy can
      # never collide with each other.
      # CI_PROJECT: distinct per-slot compose project (never the dev "process-inspector")
      # so container names can't clash and teardown only removes THIS job's containers.
      - name: Resolve the slot port namespace
        run: |
          {
            a=$(scripts/free-port.sh "${PI_ENGINE_A_PORT:-8691}" 20)
            b=$(scripts/free-port.sh "${PI_ENGINE_B_PORT:-8692}" 20 "$a")
            seven=$(scripts/free-port.sh "${PI_ENGINE_7_PORT:-8693}" 20 "$a" "$b")
            legacy=$(scripts/free-port.sh "${PI_ENGINE_LEGACY_PORT:-8694}" 20 "$a" "$b" "$seven")
            # Flap has no cross-slot-safe hardcoded default: the old `:-8695` WAS s1's
            # preferred port, so a stale runner missing PI_ENGINE_FLAP_PORT silently
            # collided with s1 (PR #364). Derive from the resolved A port (+4 = same
            # slot band) when the env is absent; recreate the slot to pick up the env.
            if [ -z "${PI_ENGINE_FLAP_PORT:-}" ]; then
              echo "WARNING: PI_ENGINE_FLAP_PORT unset — deriving $((a + 4)) from A-port band; recreate the runner slot" >&2
              flap_pref=$((a + 4))
            else
              flap_pref="$PI_ENGINE_FLAP_PORT"
            fi
            flap=$(scripts/free-port.sh "$flap_pref" 20 "$a" "$b" "$seven" "$legacy")
            pg=$(scripts/free-port.sh "${PI_PG_PORT:-5435}" 5)
            echo "PI_ENGINE_A_PORT=$a"
            echo "PI_ENGINE_B_PORT=$b"
            echo "PI_ENGINE_7_PORT=$seven"
            echo "PI_ENGINE_LEGACY_PORT=$legacy"
            echo "PI_ENGINE_FLAP_PORT=$flap"
            echo "PI_PG_PORT=$pg"
            echo "INSPECTOR_DB_URL=jdbc:postgresql://localhost:${pg}/inspector"
            echo "CI_PROJECT=pi-ci-s${PI_CI_SLOT:-1}-${{ matrix.profile }}"
          } >> "$GITHUB_ENV"
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: maven
      # Mitigates Docker Hub 429s on shared runner NAT pools; skips cleanly when the
      # secrets are absent (forks, or before they're configured).
      - name: Docker Hub login (rate-limit mitigation)
        continue-on-error: true
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      # ghcr, for the flap leg's engine. The package is private and grants THIS REPO Actions
      # read access, so the run's own GITHUB_TOKEN suffices — no long-lived PAT in a secret.
      - name: Registry login (ghcr — the flap harness image)
        if: matrix.profile == 'flap'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      # Pull EVERY run. A self-hosted runner caches images for weeks — that cache once hid a
      # missing registry permission entirely (the leg "passed" its pull without contacting
      # ghcr), and it would equally hide a months-old flap. Print what is actually under test.
      - name: Pull the flap engine (flap republishes :latest on every push to master)
        if: matrix.profile == 'flap'
        run: |
          docker pull ghcr.io/x3kcl/flap:latest
          docker image inspect ghcr.io/x3kcl/flap:latest \
            --format 'flap engine under test: {{index .RepoDigests 0}} built {{.Created}}'
      # Persistent runner: a prior interrupted run may have left CI's own containers behind.
      # Scoped to $CI_PROJECT — never touches the dev/demo projects.
      - name: Pre-clean CI harness residue
        run: docker compose -p "$CI_PROJECT" -f docker/docker-compose.dev.yml down -v --remove-orphans || true
      - name: Boot harness (${{ matrix.profile }} + postgres)
        run: docker compose -p "$CI_PROJECT" -f docker/docker-compose.dev.yml up -d
      - name: Wait for engines and postgres (bounded)
        run: bash docker/smoke-test.sh ${{ matrix.profile }}
      # --profile scopes seed to THIS leg's engines. Auto-discover would also hit any
      # other slot's reachable engines on the shared host (seen when a stale runner
      # lacked PI_ENGINE_FLAP_PORT and free-port fell back to s1's 8695 — PR #364).
      - name: Seed demo processes (REST-only, idempotent)
        run: bash docker/seed.sh --profile ${{ matrix.profile }}
      - name: Integration tests (failsafe, ${{ matrix.itClass }})
        working-directory: backend
        run: mvn -B verify -Dit.test=${{ matrix.itClass }} -Dskip.surefire.tests=true
      - name: Dump harness logs
        if: failure()
        run: docker compose -p "$CI_PROJECT" -f docker/docker-compose.dev.yml logs --no-color --timestamps
      # Always tear down CI's own project so the persistent runner leaves no residue and
      # frees the remapped ports for the next leg. Never touches dev/demo (different project).
      - name: Tear down CI harness
        if: always()
        run: docker compose -p "$CI_PROJECT" -f docker/docker-compose.dev.yml down -v --remove-orphans || true

